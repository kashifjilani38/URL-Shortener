# URL-Shortener
This Python program is a URL shortener that uses the TinyURL API to convert long URLs into shorter links. It validates the input URL, sends a request to the API using the requests library, displays the shortened URL, and handles errors gracefully.
